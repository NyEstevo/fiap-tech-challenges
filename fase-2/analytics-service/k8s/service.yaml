apiVersion: v1
kind: Service
metadata:
  name: analytics-service-svc
  namespace: tech-challenge-fase-2
  labels:
    app: analytics-service
spec:
  selector:
    app: analytics-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8005
  type: ClusterIP
