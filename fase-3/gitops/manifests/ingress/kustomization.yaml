apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Ingress unico do NGINX que roteia /<svc>/... -> <svc>-service (namespace
# toggle). Reaproveita o manifest da Fase 2 sem duplicar.
namespace: toggle
resources:
  - ../../../../fase-2/ingress.yaml
