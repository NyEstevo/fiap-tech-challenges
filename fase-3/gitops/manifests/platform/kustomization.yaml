apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Recursos de plataforma sincronizados antes dos apps de servico
# (Application 'platform' com sync-wave -1). Por ora: o ClusterSecretStore
# do External Secrets Operator apontando para o AWS Secrets Manager.
resources:
  - clustersecretstore.yaml
